// objects in TypeScript store key-value pairs

// Example
const car = {
  brand: 'Tesla',
  speed: 120,
  drive() {
    console.log(`Driving at ${this.speed} km/h`);
  },
};

car.drive();

// Notes:
// Objects can be typed using Interfaces or inline annotations
